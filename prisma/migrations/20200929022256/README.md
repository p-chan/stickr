# Migration `20200929022256`

This migration has been generated by P-Chan at 9/29/2020, 11:22:56 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Team" (
"id" text   NOT NULL ,
"teamId" text   NOT NULL ,
"raw" jsonb   NOT NULL ,
"createdAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."User" (
"id" text   NOT NULL ,
"userId" text   NOT NULL ,
"teamId" text   NOT NULL ,
"xoxsToken" text   NOT NULL ,
"createdAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."Alias" (
"id" text   NOT NULL ,
"name" text   NOT NULL ,
"originalName" text   NOT NULL ,
"teamId" text   NOT NULL ,
"createdAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE UNIQUE INDEX "Team.teamId_unique" ON "public"."Team"("teamId")

CREATE UNIQUE INDEX "User.userId_teamId_unique" ON "public"."User"("userId", "teamId")

CREATE UNIQUE INDEX "Alias.name_teamId_unique" ON "public"."Alias"("name", "teamId")

ALTER TABLE "public"."User" ADD FOREIGN KEY ("teamId")REFERENCES "public"."Team"("teamId") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."Alias" ADD FOREIGN KEY ("teamId")REFERENCES "public"."Team"("teamId") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200929022256
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,45 @@
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model Team {
+  id        String   @id @default(uuid())
+  teamId    String   @unique
+  raw       Json
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+
+  users   User[]
+  aliases Alias[]
+}
+
+model User {
+  id        String   @id @default(uuid())
+  userId    String
+  teamId    String
+  xoxsToken String
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+
+  team Team @relation(fields: [teamId], references: [teamId])
+
+  @@unique([userId, teamId])
+}
+
+model Alias {
+  id           String   @id @default(uuid())
+  name         String
+  originalName String
+  teamId       String
+  createdAt    DateTime @default(now())
+  updatedAt    DateTime @updatedAt
+
+  team Team @relation(fields: [teamId], references: [teamId])
+
+  @@unique([name, teamId])
+}
```


