# Migration `20201002001954`

This migration has been generated by P-Chan at 10/2/2020, 9:19:54 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Alias" DROP COLUMN "originalName"
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200929022256..20201002001954
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -31,14 +31,13 @@
   @@unique([userId, teamId])
 }
 model Alias {
-  id           String   @id @default(uuid())
-  name         String
-  originalName String
-  teamId       String
-  createdAt    DateTime @default(now())
-  updatedAt    DateTime @updatedAt
+  id        String   @id @default(uuid())
+  name      String
+  teamId    String
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
   team Team @relation(fields: [teamId], references: [teamId])
   @@unique([name, teamId])
```


